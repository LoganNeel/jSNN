package jsnn.jsnn.viewModels;

import jsnn.jsnn.interfaces.ViewModel;


/**
 * The `ContextViewViewModel` class implements the `ViewModel` interface
 * and serves as the view model for the context view in the application,
 * which is where users will change the values of nodes or synapses by hand.
 *
 * This class is responsible for managing the state and behavior of the
 * context view, providing data and logic to the view layer.
 */
public class ContextViewViewModel implements ViewModel {

    public ContextViewViewModel(){

    }

}
